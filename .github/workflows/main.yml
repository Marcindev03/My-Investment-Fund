name: PR opened

on:
  pull_request:
    types: [opened]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Use Node.js 16.x
      uses: actions/setup-node@v1
      with:
          node-version: '16.x'
          cache: 'yarn'
      
    - name: Install dependencies
      run: yarn

    - name: Build app
      run: yarn build
